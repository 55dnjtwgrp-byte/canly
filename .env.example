# Copy this file to .env.local and fill in your Supabase project's values
# (Project Settings > API in the Supabase dashboard) to enable shared pins.
# Without these, Canly runs entirely local-only — pins just aren't shared.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
